apiVersion: v1
kind: Service
metadata:
  labels:
    app: clio-ui-service-production
  name: clio-ui-service-production
spec:
  ports:
    - name: http
      port: 8080
  selector:
    app: clio-ui-production
  type: ClusterIP
