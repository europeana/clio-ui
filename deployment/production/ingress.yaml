apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clio-ui-ingress-production
  annotations:
    cert-manager.io/issuer: letsencrypt-production
spec:
  ingressClassName: public-iks-k8s-nginx
  tls:
    - hosts:
        - clio-ui.production.eanadev.org
      secretName: clio-ui.production.eanadev.org
  rules:
    - host: clio-ui.production.eanadev.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: clio-ui-service-production
                port:
                  number: 8080
