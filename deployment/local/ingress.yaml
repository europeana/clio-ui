apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clio-ui-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: clio-ui-service
                port:
                  number: 8080
